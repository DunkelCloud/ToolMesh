backends:
  # MCP reference server (uncomment if running mcp/everything in docker-compose)
  # Note: mcp/everything has SSE stability issues — crashes on idle timers.
  # - name: everything
  #   transport: http
  #   url: "http://everything:3001/sse"

  # Memorizer — personal knowledge base with semantic search
  - name: memorizer
    transport: http
    url: "https://memorizer.dunkel.cloud/mcp"
    api_key_env: "MEMORIZER_API_KEY"

  # Example: connect to a local MCP server via STDIO
  # - name: local-tools
  #   transport: stdio
  #   command: "./my-mcp-server"
  #   args: []
